<Page x:Class="SGA_Desktop.Views.OrdenTraspasoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:SGA_Desktop.ViewModels"
      mc:Ignorable="d"
      Background="{StaticResource BrushFondo}">

    <Page.DataContext>
        <vm:OrdenTraspasoViewModel />
    </Page.DataContext>

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0" Text="Órdenes de Traspaso" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="{StaticResource BrushPrincipal}"
                   Margin="0,0,0,20"/>

        <!-- Lista de órdenes -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding OrdenesTraspaso}" 
                  SelectedItem="{Binding OrdenSeleccionada}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdOrdenTrabajo}" Width="100"/>
                <DataGridTextColumn Header="Empresa" Binding="{Binding CodigoEmpresa}" Width="80"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                <DataGridTextColumn Header="Prioridad" Binding="{Binding Prioridad}" Width="80"/>
                <DataGridTextColumn Header="Fecha Plan" Binding="{Binding FechaPlan, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Usuario Asignado" Binding="{Binding UsuarioAsignado}" Width="120"/>
                <DataGridTextColumn Header="Comentarios" Binding="{Binding Comentarios}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botón Nueva Orden -->
        <Button Grid.Row="2" Content="Nueva Orden" Command="{Binding CrearOrdenCommand}" 
                Style="{StaticResource EstiloBotonPrincipal}" 
                HorizontalAlignment="Right" Margin="0,20,0,0"/>

        <!-- Loading indicator -->
        <Border Grid.Row="0" Grid.RowSpan="3" 
                Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Cargando..." Foreground="White" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</Page> 
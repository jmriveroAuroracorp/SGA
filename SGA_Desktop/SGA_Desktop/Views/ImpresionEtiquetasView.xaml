<Page x:Class="SGA_Desktop.Views.ImpresionEtiquetasView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:SGA_Desktop.ViewModels"
      Background="{StaticResource BrushFondo}"
      FontFamily="{StaticResource FuentePrincipal}">

    <DockPanel>

        <!-- Encabezado corporativo -->
        <Border Background="{StaticResource BrushPrincipal}"
                DockPanel.Dock="Top"
                Height="80">
            <DockPanel VerticalAlignment="Center">
                <TextBlock Text="Impresión de etiquetas"
                           Foreground="White"
                           FontFamily="{StaticResource FuentePrincipal}"
                           FontSize="30"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="20,0"/>
            </DockPanel>
        </Border>

        <!-- Contenido principal -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Búsqueda de artículo -->
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBox Width="250"
                         Style="{StaticResource EstiloCajaTexto}"
                         Text="{Binding FiltroArticulo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Buscar artículo"
                        Style="{StaticResource EstiloBotonPrincipal}"
                        Command="{Binding BuscarArticuloCommand}"/>
            </StackPanel>

            <!-- Listado de stock con partidas -->
            <DataGrid Grid.Row="1"
                      Margin="0,10,0,0"
                      Style="{StaticResource EstiloDataGrid}"
                      ItemsSource="{Binding Articulos}"
                      SelectedItem="{Binding SelectedArticulo, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Artículo"
                                        Binding="{Binding CodigoArticulo}" Width="*" />
                    <DataGridTextColumn Header="Descripción"
                                        Binding="{Binding DescripcionArticulo}" Width="2*" />
                    <DataGridTextColumn Header="Partida"
                                        Binding="{Binding Partida}" Width="*" />
                    <DataGridTextColumn Header="Almacén"
                                        Binding="{Binding CodigoAlmacen}" Width="*" />
                    <DataGridTextColumn Header="Ubicación"
                                        Binding="{Binding Ubicacion}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- Selección de lote y acción de impresión -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                
                <Button Content="Imprimir etiqueta"
                        Style="{StaticResource EstiloBotonPrincipal}"
                        Command="{Binding ImprimirEtiquetaCommand}"
                        IsEnabled="{Binding CanImprimir}" />
            </StackPanel>
        </Grid>

    </DockPanel>
</Page>

<Window x:Class="SGA_Desktop.Dialog.PaletCrearDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Código -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Código:" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
        <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource EstiloCajaTexto}"/>

        <!-- Tipo de palet -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Tipo de palet:" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1"
              ItemsSource="{Binding TiposPaletDisponibles}"
              SelectedItem="{Binding TipoPaletSeleccionado}"
              DisplayMemberPath="Descripcion"
              Style="{StaticResource EstiloComboBox}"/>


        <!-- Altura -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Altura (cm):" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
        <TextBox   Grid.Row="3" Grid.Column="1" Text="{Binding Altura}" Style="{StaticResource EstiloCajaTexto}"/>

        <!-- Peso -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Peso (kg):" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
        <TextBox   Grid.Row="4" Grid.Column="1" Text="{Binding Peso}" Style="{StaticResource EstiloCajaTexto}"/>

        <!-- Botones -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="100" Margin="0,0,8,0" IsCancel="True" Style="{StaticResource EstiloBotonPrincipal}"/>
            <Button Content="Crear" Width="100" IsDefault="True" Command="{Binding CrearCommand}" Style="{StaticResource EstiloBotonPrincipal}"/>
        </StackPanel>
    </Grid>
</Window>

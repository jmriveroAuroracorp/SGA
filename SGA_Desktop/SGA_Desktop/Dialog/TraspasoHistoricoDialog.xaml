<Window x:Class="SGA_Desktop.Dialog.TraspasoHistoricoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Helpers="clr-namespace:SGA_Desktop.Helpers"
        xmlns:converters="clr-namespace:SGA_Desktop.Converters"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        Title="Historial de Traspasos"
        Width="1400"
        Height="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <!-- Carga de estilos globales -->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/SGA_Desktop;component/Resources/Estilos.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- 🚀 Converters para optimización de rendimiento -->
            <converters:TipoTraspasoToColorConverter x:Key="TipoTraspasoToColorConverter"/>
            <converters:TipoTraspasoToTextConverter x:Key="TipoTraspasoToTextConverter"/>
            <converters:EstadoToColorConverter x:Key="EstadoToColorConverter"/>
            <converters:EstadoToTextConverter x:Key="EstadoToTextConverter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="{StaticResource BrushFondo}"
            BorderBrush="{StaticResource BrushPrincipal}"
            BorderThickness="2"
            Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Filtros - más compactos -->
                <RowDefinition Height="*"/> <!-- Tabla de traspasos - ocupa todo el espacio disponible -->
                <RowDefinition Height="Auto"/> <!-- Botones -->
            </Grid.RowDefinitions>

            <!-- Filtros - más compactos -->
            <GroupBox Header="Filtros" Grid.Row="0" Margin="0,0,0,16">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Primera fila de filtros - más compacta -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="Fecha desde:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <DatePicker SelectedDate="{Binding FechaDesde}" Style="{StaticResource EstiloDatePicker}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="5,0">
                        <TextBlock Text="Fecha hasta:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <DatePicker SelectedDate="{Binding FechaHasta}" Style="{StaticResource EstiloDatePicker}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="5,0">
                        <TextBlock Text="Código artículo:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding CodigoArticulo, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource EstiloCajaTexto}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="3" Margin="5,0">
                        <TextBlock Text="Estado:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding Estados}"
                                  SelectedItem="{Binding EstadoSeleccionado}"
                                  DisplayMemberPath="Descripcion"
                                  Style="{StaticResource EstiloComboBox}" Height="30"/>
                    </StackPanel>

                    <!-- Segunda fila de filtros -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,10,10,0">
                        <TextBlock Text="Almacén origen:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding AlmacenesOrigenView}"
                                  SelectedItem="{Binding AlmacenOrigenSeleccionado}"
                                  Text="{Binding FiltroAlmacenesOrigen, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayMemberPath="DescripcionCombo"
                                  Style="{StaticResource EstiloComboBox}" Height="30"
                                  IsEditable="True"
                                  IsTextSearchEnabled="False"
                                  StaysOpenOnEdit="True"
                                  IsDropDownOpen="{Binding IsDropDownOpenAlmacenesOrigen}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="GotFocus">
                                    <i:InvokeCommandAction Command="{Binding AbrirDropDownAlmacenesOrigenCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding CerrarDropDownAlmacenesOrigenCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding LimpiarSeleccionAlmacenesOrigenCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,10,5,0">
                        <TextBlock Text="Almacén destino:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding AlmacenesDestinoView}"
                                  SelectedItem="{Binding AlmacenDestinoSeleccionado}"
                                  Text="{Binding FiltroAlmacenesDestino, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayMemberPath="DescripcionCombo"
                                  Style="{StaticResource EstiloComboBox}" Height="30"
                                  IsEditable="True"
                                  IsTextSearchEnabled="False"
                                  StaysOpenOnEdit="True"
                                  IsDropDownOpen="{Binding IsDropDownOpenAlmacenesDestino}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="GotFocus">
                                    <i:InvokeCommandAction Command="{Binding AbrirDropDownAlmacenesDestinoCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding CerrarDropDownAlmacenesDestinoCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding LimpiarSeleccionAlmacenesDestinoCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="5,10,5,0">
                        <TextBlock Text="Operario:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding OperariosView}"
                                  DisplayMemberPath="NombreOperario"
                                  SelectedItem="{Binding OperarioSeleccionado}"
                                  Text="{Binding FiltroOperarios, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  IsDropDownOpen="{Binding IsDropDownOpenOperarios}"
                                  Style="{StaticResource EstiloComboBox}" Height="30"
                                  IsEditable="True"
                                  IsTextSearchEnabled="False"
                                  StaysOpenOnEdit="True"
                                  MaxDropDownHeight="200">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding CerrarDropDownOperariosCommand}"/>
                                </i:EventTrigger>
                                <i:EventTrigger EventName="GotFocus">
                                    <i:InvokeCommandAction Command="{Binding AbrirDropDownOperariosCommand}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ComboBox>
                    </StackPanel>

                    <!-- Botones de filtros en la segunda fila -->
                    <StackPanel Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="🔄 Recargar" 
                                Command="{Binding AplicarFiltrosCommand}"
                                Style="{StaticResource EstiloBotonPrincipal}" 
                                Width="100" Height="30" Margin="0,0,8,0"/>
                        <Button Content="🧹 Limpiar" 
                                Command="{Binding LimpiarFiltrosCommand}"
                                Style="{StaticResource EstiloBotonSecundario}" 
                                Width="80" Height="30"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Indicador de carga -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock Text="Cargando..." 
                           Foreground="{StaticResource BrushPrincipal}" 
                           FontWeight="Bold" 
                           Visibility="{Binding EstaCargando, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>

            <!-- Historial de traspasos con formato de cards -->
            <GroupBox Header="Historial de Traspasos" Grid.Row="1" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Contador de resultados -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,20,10">
                        <TextBlock Text="{Binding Traspasos.Count, StringFormat='{}{0} traspasos encontrados'}" 
                                   Foreground="{StaticResource BrushPrincipal}" 
                                   FontWeight="Bold" 
                                   FontSize="12"/>
                    </StackPanel>
                    
                    <!-- Lista de traspasos - OPTIMIZADO con ListBox virtualizado -->
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding Traspasos}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Margin="15,0,15,15"
                             Background="Transparent"
                             BorderThickness="0"
                             Focusable="False">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="IsSelected" Value="False"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White"
                                                CornerRadius="6" 
                                                Padding="12" 
                                                BorderThickness="1" 
                                                BorderBrush="{StaticResource BrushIntermedio}"
                                                Cursor="Hand">
                                            <!-- 🚀 OPTIMIZACIÓN: Efecto simplificado para mejor rendimiento -->
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.1"/>
                                            </Border.Effect>
                                            
                                            <!-- Layout compacto de dos filas -->
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/> <!-- Primera fila: Información principal -->
                                                    <RowDefinition Height="Auto"/> <!-- Segunda fila: Información secundaria -->
                                                </Grid.RowDefinitions>
                                                
                                                <!-- Primera fila: Fecha, Artículo, Descripción, Tipo, Estado, Cantidad -->
                                                <Grid Grid.Row="0" Margin="0,0,0,6">
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="130"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="80"/>
                                                        <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="100"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <!-- Fecha -->
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="FECHA" FontSize="11" FontWeight="Bold" 
                                                                    Foreground="Gray" Margin="0,0,0,3"/>
                                                    <TextBlock Text="{Binding FechaInicio, StringFormat=dd/MM/yyyy HH:mm}" 
                                                               FontSize="13" FontWeight="Bold" 
                                                               Foreground="{StaticResource BrushPrincipal}"/>
                                                </StackPanel>
                                                
                                                <!-- Artículo -->
                                                     <StackPanel Grid.Column="1" Margin="6,0,0,0">
                                                    <TextBlock Text="ARTÍCULO" FontSize="11" FontWeight="Bold" 
                                                                    Foreground="Gray" Margin="0,0,0,3"/>
                                                    <TextBlock Text="{Binding CodigoArticulo}" FontSize="13" FontWeight="Bold" 
                                                               Foreground="{StaticResource BrushPrincipal}"/>
                                                </StackPanel>
                                                
                                                <!-- Descripción -->
                                                     <StackPanel Grid.Column="2" Margin="6,0,0,0">
                                                    <TextBlock Text="DESCRIPCIÓN" FontSize="11" FontWeight="Bold" 
                                                                    Foreground="Gray" Margin="0,0,0,3"/>
                                                         <TextBlock Text="{Binding DescripcionArticulo}" FontSize="12" 
                                                               Foreground="Black" TextWrapping="Wrap" 
                                                                    FontWeight="Medium" MaxHeight="36" TextTrimming="CharacterEllipsis"/>
                                                     </StackPanel>
                                                    
                                                    <!-- Tipo de Traspaso - OPTIMIZADO -->
                                                    <StackPanel Grid.Column="3" Margin="6,0,0,0">
                                                        <TextBlock Text="TIPO" FontSize="11" FontWeight="Bold" 
                                                                   Foreground="Gray" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                                        <!-- 🚀 OPTIMIZACIÓN: Simplificado sin efectos pesados -->
                                                        <Border CornerRadius="6" Padding="6,3" HorizontalAlignment="Center" 
                                                                Background="{Binding TipoTraspaso, Converter={StaticResource TipoTraspasoToColorConverter}}">
                                                            <TextBlock FontSize="9" FontWeight="Bold" 
                                                                       HorizontalAlignment="Center" Foreground="White"
                                                                       Text="{Binding TipoTraspaso, Converter={StaticResource TipoTraspasoToTextConverter}}"/>
                                                        </Border>
                                                    </StackPanel>
                                                    
                                                    <!-- Estado - OPTIMIZADO -->
                                                    <StackPanel Grid.Column="4" Margin="6,0,0,0">
                                                        <TextBlock Text="ESTADO" FontSize="11" FontWeight="Bold" 
                                                                   Foreground="Gray" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                                        <!-- 🚀 OPTIMIZACIÓN: Simplificado sin efectos pesados -->
                                                        <Border CornerRadius="6" Padding="6,3" HorizontalAlignment="Center" 
                                                                Background="{Binding CodigoEstado, Converter={StaticResource EstadoToColorConverter}}">
                                                            <TextBlock FontSize="9" FontWeight="Bold" 
                                                                       HorizontalAlignment="Center" Foreground="White"
                                                                       Text="{Binding CodigoEstado, Converter={StaticResource EstadoToTextConverter}}"/>
                                                        </Border>
                                                    </StackPanel>
                                                    
                                                    <!-- Cantidad -->
                                                    <StackPanel Grid.Column="5" Margin="6,0,0,0">
                                                        <TextBlock Text="CANTIDAD" FontSize="11" FontWeight="Bold" 
                                                                   Foreground="Gray" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                                        <TextBlock Text="{Binding Cantidad, StringFormat=F2}" FontSize="13" 
                                                                   FontWeight="Bold" Foreground="{StaticResource BrushPrincipal}" 
                                                                   HorizontalAlignment="Center"/>
                                                </StackPanel>
                                                </Grid>
                                                
                                                <!-- Segunda fila: Origen, Destino, Palet, Usuario, Observaciones -->
                                                <Grid Grid.Row="1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="100"/>
                                                        <ColumnDefinition Width="150"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                
                                                <!-- Origen -->
                                                     <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                                    <TextBlock Text="ORIGEN" FontSize="11" FontWeight="Bold" 
                                                                     Foreground="Gray" Margin="0,0,0,3"/>
                                                    <TextBlock Text="{Binding AlmacenOrigen}" FontSize="12" 
                                                               FontWeight="Bold" Foreground="{StaticResource BrushPrincipal}"/>
                                                    <TextBlock Text="{Binding UbicacionOrigen}" FontSize="11" 
                                                               Foreground="Black" FontWeight="Medium"/>
                                                </StackPanel>
                                                
                                                <!-- Destino -->
                                                     <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                                    <TextBlock Text="DESTINO" FontSize="11" FontWeight="Bold" 
                                                                     Foreground="Gray" Margin="0,0,0,3"/>
                                                    <TextBlock Text="{Binding AlmacenDestino}" FontSize="12" 
                                                               FontWeight="Bold" Foreground="{StaticResource BrushPrincipal}"/>
                                                    <TextBlock Text="{Binding UbicacionDestino}" FontSize="11" 
                                                               Foreground="Black" FontWeight="Medium"/>
                                                </StackPanel>
                                                
                                                    <!-- Palet -->
                                                    <StackPanel Grid.Column="2" Margin="0,0,8,0">
                                                        <TextBlock Text="PALET" FontSize="11" FontWeight="Bold" 
                                                                   Foreground="Gray" Margin="0,0,0,3" HorizontalAlignment="Center"/>
                                                        <TextBlock FontSize="11" FontWeight="Bold" 
                                                                   HorizontalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Text" Value="Sin paletizar"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding CodigoPalet}" Value="{x:Null}">
                                                                            <Setter Property="Text" Value="Sin paletizar"/>
                                                                            <Setter Property="Foreground" Value="#6C757D"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding CodigoPalet}" Value="">
                                                                            <Setter Property="Text" Value="Sin paletizar"/>
                                                                            <Setter Property="Foreground" Value="#6C757D"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding CodigoPalet}" Value="NULL">
                                                                            <Setter Property="Text" Value="Sin paletizar"/>
                                                                            <Setter Property="Foreground" Value="#6C757D"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                            <TextBlock.Text>
                                                                <MultiBinding StringFormat="{}{0}">
                                                                    <Binding Path="CodigoPalet"/>
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                </StackPanel>
                                                
                                                 <!-- Usuario -->
                                                     <StackPanel Grid.Column="3" Margin="0,0,8,0">
                                                          <TextBlock Text="USUARIO" FontSize="11" FontWeight="Bold" 
                                                                     Foreground="Gray" Margin="0,0,0,3"/>
                                                          <TextBlock Text="{Binding UsuarioInicioNombre}" FontSize="11" 
                                                                   Foreground="Black" FontWeight="Medium" 
                                                                   TextWrapping="Wrap" MaxHeight="24" TextTrimming="CharacterEllipsis"/>
                                                 </StackPanel>
                                                 
                                                    <!-- Observaciones -->
                                                    <StackPanel Grid.Column="4">
                                                        <TextBlock Text="OBSERVACIONES" FontSize="11" FontWeight="Bold" 
                                                                   Foreground="Gray" Margin="0,0,0,3"/>
                                                        <TextBlock FontSize="11" Foreground="Black" FontWeight="Medium" 
                                                                   TextWrapping="Wrap" MaxHeight="24" TextTrimming="CharacterEllipsis">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Text" Value="Sin observaciones"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Comentarios}" Value="{x:Null}">
                                                                            <Setter Property="Text" Value="Sin observaciones"/>
                                                                            <Setter Property="Foreground" Value="#6C757D"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Comentarios}" Value="">
                                                                            <Setter Property="Text" Value="Sin observaciones"/>
                                                                            <Setter Property="Foreground" Value="#6C757D"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                            <TextBlock.Text>
                                                                <Binding Path="Comentarios"/>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                </StackPanel>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </GroupBox>

            <!-- Botones -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Cerrar" Width="120" Height="35" 
                        IsCancel="True" Command="{Binding CerrarCommand}" 
                        Style="{StaticResource EstiloBotonPrincipal}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window> 